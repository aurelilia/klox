// This is a file for testing the interpreter.

/* Multiline comments!
    /* Nested, even. */
    Neat.
*/

var num = (5 + 5) - 23;

if (num == 0 or num > 5) {
    print("Good");
} else {
    print("bad.");
}

for (var i = 0; i < 10; i = i + 1) {
    printLine(i);
}

print(clock());

class Hello {}
print(Hello())